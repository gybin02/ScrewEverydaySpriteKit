interface:
  display_name: "Yueban Image to Code"
  short_description: "750px pixel-level image-to-code and PNG slicing"
  default_prompt: "Use $yueban-image-to-code to convert the selected UI image into code and export precise transparent PNG assets."
